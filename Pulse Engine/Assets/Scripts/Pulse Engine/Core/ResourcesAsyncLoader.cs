using System;
using UnityEngine;

namespace PulseEngine
{

    /// <summary>
    /// The monobehaviour attached to the singleton dont't destroy on load, Resource loader gamaobject.
    /// </summary>
    public class ResourcesAsyncLoader: MonoBehaviour
    {
    }
}
